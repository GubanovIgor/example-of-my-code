import { GUTTER_SIZE } from 'config/theme';

import { SCREEN_WIDTH } from 'constants/DIMENSIONS';

export const BONUS_WIDTH = SCREEN_WIDTH - GUTTER_SIZE * 6;
export const BONUS_HEIGHT = BONUS_WIDTH / 2.34;
export const COLORED_BOX_WIDTH = BONUS_WIDTH / 2.1;
export const COLORED_BOX_HEIGHT = COLORED_BOX_WIDTH / 1.9;
