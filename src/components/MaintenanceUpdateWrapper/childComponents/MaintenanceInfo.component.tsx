import React, { FC } from 'react';
import { View } from 'react-native';

import { IMatinenance } from '../MaintenanceUpdateWrapper.types';

interface Props {
  info?: IMatinenance;
}
export const MaintenanceInfo: FC<Props> = ({}) => {
  return <View></View>;
};
